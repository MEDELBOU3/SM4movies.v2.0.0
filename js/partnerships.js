 // Initialize AOS (if used)
        AOS.init({
            duration: 700,
            once: true,
            offset: 100,
            easing: 'ease-out-quad',
        });

        // Add any page-specific JS here (e.g., form validation enhancement)
